import { NextResponse } from "next/server";
import { z } from "zod";
import { MediaRightsSchema } from "@/lib/video-domain";
import { RequestContextError, requireWorkspaceContext } from "@/lib/request-context";

const UploadSchema=z.object({filename:z.string().min(1).max(180),contentType:z.enum(["image/jpeg","video/mp4","video/quicktime","video/webm","video/x-m4v"]),byteSize:z.number().int().positive().max(524288000),rights:MediaRightsSchema});
function extension(filename:string,contentType:string){if(contentType==="image/jpeg")return"jpg";const value=filename.split(".").at(-1)?.toLowerCase()??"mp4";return/^(mp4|mov|webm|m4v)$/.test(value)?value:"mp4";}
export async function POST(request:Request){try{const input=UploadSchema.parse(await request.json());if(input.rights.isCustomerMedia&&(!input.rights.approvedPlatforms.includes("instagram")||input.rights.consentStatus!=="approved"))return NextResponse.json({error:"お客様素材はInstagram利用同意が必要です。"},{status:409});const{supabase,userId,workspaceId}=await requireWorkspaceContext();const assetId=crypto.randomUUID();const path=`${workspaceId}/${assetId}/original.${extension(input.filename,input.contentType)}`;const{error:assetError}=await supabase.from("media_assets").insert({id:assetId,workspace_id:workspaceId,storage_bucket:"publish-assets",storage_key:path,original_filename:input.filename,media_type:input.contentType,byte_size:input.byteSize,metadata:{source:"PHASE3_PUBLISH_UPLOAD",integrity:"UNVERIFIED"},is_customer_media:input.rights.isCustomerMedia,created_by:userId});if(assetError)throw assetError;const{error:rightsError}=await supabase.from("media_asset_rights").insert({workspace_id:workspaceId,media_asset_id:assetId,consent_status:input.rights.consentStatus,approved_platforms:input.rights.approvedPlatforms,approved_usage:input.rights.approvedUsage,music_license_status:input.rights.musicLicenseStatus,expires_at:input.rights.expiresAt});if(rightsError)throw rightsError;const{data,error:signedError}=await supabase.storage.from("publish-assets").createSignedUploadUrl(path);if(signedError)throw signedError;return NextResponse.json({assetId,path,token:data.token});}catch(error){if(error instanceof RequestContextError)return NextResponse.json({error:error.message},{status:error.status});if(error instanceof z.ZodError)return NextResponse.json({error:"投稿素材の形式が不正です。"},{status:400});console.error("instagram_media_upload_prepare_failed",error instanceof Error?error.name:"unknown");return NextResponse.json({error:"投稿素材のアップロードを準備できませんでした。"},{status:500});}}
